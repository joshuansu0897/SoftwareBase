{
    "saveAndRunExt": {
        "commands": [
            {
                "match": "\\.asm$",
                "cmd": "bash ${workspaceRoot}/autoCompile ${fileDirname} ${fileBasenameNoExt}"
            }
        ]
    }
}