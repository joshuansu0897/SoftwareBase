{
    "saveAndRunExt": {
        "commands": [
            {
                "match": "\\.asm$",
                "cmd": "bash ${workspaceRoot}/autoCompile.sh ${fileDirname} ${fileBasenameNoExt}"
            }
        ]
    }
}