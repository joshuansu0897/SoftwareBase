{
    "saveAndRunExt": {
        "commands": [
            {
                "match": "\\.asm$",
                "cmd": "bash autoCompile.sh ${fileDirname} ${fileBasenameNoExt}"
            }
        ]
    }
}